{
  "name": "agmsg-qwencode-plugin",
  "version": "2.0.0",
  "description": "agmsg MCP server for Qwen Code — agent-to-agent messaging over SQLite",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "monitor": "bun run index.ts monitor"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "bun-types": "^1.3.0"
  }
}
