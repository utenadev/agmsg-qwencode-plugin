{
  "name": "agmsg-qwencode-plugin",
  "version": "1.0.0",
  "description": "Native SQLite3 agmsg integration for Qwen Code — send + inbox via bun:sqlite",
  "main": "index.ts",
  "type": "module",
  "keywords": [
    "qwen-code",
    "plugin",
    "agmsg",
    "sqlite",
    "multi-agent"
  ],
  "license": "MIT",
  "scripts": {
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:e2e": "./scripts/e2e.sh",
    "typecheck": "bun x tsc --noEmit"
  },
  "dependencies": {
    "agmsg-common-plugin": "file:../agmsg-common-plugin"
  },
  "devDependencies": {
    "bun-types": "^1.3.0",
    "typescript": "^5.3.3"
  }
}
